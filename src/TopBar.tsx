import { Box } from "@mui/material";

const TopBar = () => {
    return (
        <Box sx={{ backgroundColor: '#007FFF', color: '#FFFFFF' }}>
            Top bar
        </Box>)
}

export default TopBar;